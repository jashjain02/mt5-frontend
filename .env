# Local development — points to the local FastAPI server.
# CI/CD production build overrides this via:
#   VITE_API_URL: https://<DOMAIN>  (set in deploy.yml build-frontend job)
# Never commit a production URL here — it would break all local dev work.
VITE_API_URL=http://localhost:8000
